export{}

const firstName = "Orlandi"

const lastName = 3

const y = true

function greeting(name: string) {
    console.log("Olá " + name)
}

greeting(firstName)
// greeting(lastName)
// greeting(y)
